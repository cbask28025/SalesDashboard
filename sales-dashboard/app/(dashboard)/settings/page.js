export const metadata = { title: 'Settings — CTB Sales Dashboard' }

export default function SettingsPage() {
  return (
    <div className="page-shell">
      <div className="page-heading">
        <h2>Settings</h2>
        <p>Sequence timing, hot-lead thresholds, sending rules, Outlook connection, assistant prompt, notification prefs. Lands in Phase H.</p>
      </div>
      <div className="page-placeholder">Phase H</div>
    </div>
  )
}
